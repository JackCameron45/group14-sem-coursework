sudo: required

language: java

services:
  - docker

after_success:
  - docker network create --driver bridge group14-sem-coursework
  - docker pull mongo
  - docker run -d --name mongo-dbserver --network group14-sem-coursework mongo
  - docker build -t group14-sem-coursework
  - docker run --network group14-sem-coursework group14-sem-coursework
